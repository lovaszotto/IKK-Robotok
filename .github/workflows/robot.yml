name: Hello World Workflow

on:
  push:
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "Hello, world!"
      
  hello2:
      runs-on: ubuntu-latest
      steps:
        - name: Print hello2
          run: echo "Hello2, world!"
